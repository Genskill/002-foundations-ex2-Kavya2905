#include <stdio.h>
#include <cs50.h>

int main() {
    int a,b,c;
    
    a = get_float("Enter number 1\n");
    b = get_float("Enter number 2\n");
    c = get_float("Enter number 3\n");
    
    if(a*a + b*b = c*c || b*b+c*c = a*a || a*a + c*c = b*b){
        printf("Yes");
    }
    else
    {
        printf("No");
    }
    
    return 0;
}
